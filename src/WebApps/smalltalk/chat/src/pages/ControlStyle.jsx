import React from 'react';
import STButton from "../components/UI/STButton";

const ControlStyle = () => {
    return (
        <div>
            <STButton/>
        </div>
    );
};

export default ControlStyle;