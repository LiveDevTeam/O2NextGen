apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "idportal-mvc.fullname" . }}-test-connection"
  labels:
    {{- include "idportal-mvc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "idportal-mvc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
