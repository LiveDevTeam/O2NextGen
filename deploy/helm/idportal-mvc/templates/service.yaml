apiVersion: v1
kind: Service
metadata:
  name: {{ include "idportal-mvc.fullname" . }}
  labels:
    {{- include "idportal-mvc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "idportal-mvc.selectorLabels" . | nindent 4 }}
