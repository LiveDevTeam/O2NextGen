apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "idportal-api.fullname" . }}-test-connection"
  labels:
    {{- include "idportal-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "idportal-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
