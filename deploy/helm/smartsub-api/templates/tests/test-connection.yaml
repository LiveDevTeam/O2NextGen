apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "smartsub-api.fullname" . }}-test-connection"
  labels:
    {{- include "smartsub-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "smartsub-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
