apiVersion: v1
kind: Service
metadata:
  name: {{ include "smartsub-api.fullname" . }}
  labels:
    {{- include "smartsub-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "smartsub-api.selectorLabels" . | nindent 4 }}
